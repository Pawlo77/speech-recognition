[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "speech-recognition"
version = "0.1.0"
requires-python = ">=3.12.4,<3.12.5"

dependencies = [
    "dotenv>=0.9.9",
    "fvcore>=0.1.5.post20221221",
    "humanize>=4.15.0",
    "ipykernel>=7.2.0",
    "ipywidgets>=8.1.8",
    "kagglehub>=0.3.6",
    "librosa>=0.11.0",
    "mamba-ssm-macos>=1.0.1",
    "matplotlib>=3.10.8",
    "mlflow>=1.27.0",
    "mlflow-sysmetrics>=0.1.3",
    "nbformat>=5.10.4",
    "numpy>=2.4.2",
    "pandas>=2.2.0",
    "pip>=26.0.1",
    "plotly>=6.7.0",
    "psutil>=7.2.2",
    "py7zr>=1.1.0",
    "pytest>=9.0.2",
    "python-dotenv>=1.2.2",
    "scipy>=1.17.1",
    "seaborn>=0.13.2",
    "timm>=1.0.26",
    "torch>=2.8.0",
    "torchaudio>=2.8.0",
    "torchvision>=0.26.0",
    "tqdm>=4.67.0",
    "transformers>=5.5.4",
    "xlstm>=2.0.5",
]

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
    "ruff>=0.12.0",
]

[project.scripts]
speech-recognition = "speech_recognition.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py312"
line-length = 100
exclude = [
    ".git",
    ".venv",
    "build",
    "dist",
]

[tool.ruff.lint]
select = [
    "E",
    "F",
    "W",
    "I",
    "N",
    "UP",
    "B",
    "C4",
    "SIM",
    "RET",
    "ARG",
    "PIE",
    "PTH",
    "RUF",
    "S",
]
ignore = [
    "S101",
]
fixable = ["ALL"]

[tool.ruff.lint.mccabe]
max-complexity = 12

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pytest.ini_options]
pythonpath = ["src"]
